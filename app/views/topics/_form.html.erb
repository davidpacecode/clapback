<%= form_with(model: topic) do |form| %>
  <% if topic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
        <% tpoic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="wa-stack">
    <h3 class="wa-heading-m">Topic Info</h3>
    <p class="wa-caption-m">Details about our Company's perspective</p>
    <wa-divider></wa-divider>
    <dl class="wa-stack wa-gap-2xl">
      <div class="wa-flank" style="--flank-size: 20ch;">
        <dt><%= form.label :question, style: "display: block" %></dt>
        <dd><%= form.text_field :question %></dd>
      </div>
      <div class="wa-flank wa-align-items-start" style="--flank-size: 20ch;">
        <dt><%= form.label :category, style: "display: block" %></dt>
        <dd>
          <%= form.text_field :category %>
        </dd>
      </div>
      <div class="wa-flank" style="--flank-size: 20ch;">
        <dt><%= form.label :tags, style: "display: block" %></dt>
        <dd><%= form.text_field :tags %></dd>
      </div>
      <div class="wa-flank" style="--flank-size: 20ch;">
        <dt><%= form.label :messaging, style: "display: block" %></dt>
        <dd><%= form.rich_text_area :messaging %></dd>
      </div>
      <%= tag.br %>
      <%= form.submit "Save" %>
    </dl>
  </div>
<% end %>

<% if false %>
  <div>
    <%= form.label :question, style: "display: block" %>
    <%= form.textarea :question %>
  </div>

  <div>
    <%= form.label :messaging, style: "display: block" %>
    <%= form.rich_text_area :messaging %>
  </div>

  <div>
    <%= form.label :tags, style: "display: block" %>
    <%= form.textarea :tags %>
  </div>

  <div>
    <%= form.label :category, style: "display: block" %>
    <%= form.text_field :category %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
